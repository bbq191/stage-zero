const DEFAULT_CAPCITY: i32 = 10;

struct array_list {
    size: i32,
    elements: [i32; 10],
}

impl array_list {
    pub fn add() {
        println!("quick")
    }

    pub fn get(index: i32) -> i32 {
        return;
    }
}
